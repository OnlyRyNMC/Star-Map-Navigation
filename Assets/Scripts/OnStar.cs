using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class OnStar : MonoBehaviour {
    // Dicstionary of the star routes
    public Dictionary<OnStar, float> DicStarRoutes = new Dictionary<OnStar, float>();
    // Start is called before the first frame update
    void Start(){
            
    }

    // Update is called once per frame
    void Update(){
        
    }

}
